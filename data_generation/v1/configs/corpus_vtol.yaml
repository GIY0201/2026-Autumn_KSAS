seed: 17
profile_id: vtol_full_flight_v1
mode: corpus
balance:
  strategy: phase_direction_marginal_v1
  split_episode_counts:
    train: 112
    validation: 24
    test: 24
source_motion_check_status: NOT_RUN
training:
  window_samples: 91
  train_windows_per_episode: 8
  evaluation_stride_samples: 75
  behavior_balance:
    schema: behavior-window-balance-v1
    core_windows_per_behavior: 256
    transition_windows_per_boundary: 1
  identity:
    transition_margin_s: 3.0
ui:
  label: "VTOL · 모든 시나리오 + 학습데이터"
  description: "모든 기동 조합을 train/valid/test로 저장합니다. 관측오차 3종 × 식별 획득·항상 미식별·식별 상실 3종을 생성하며 모델 학습은 실행하지 않습니다."
hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
