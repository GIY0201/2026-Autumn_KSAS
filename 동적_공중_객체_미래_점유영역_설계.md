# 동적 공중 객체의 미래 3차원 점유 영역 예측 설계

## 1. 문서의 범위

이 문서는 기존 프로그램과 이전 V2 설계를 이어받지 않고, 새로 구성할 연구 방법에 대해 현재 대화에서 결정한 사항만 정리한다. 구현 명세가 아니라 논문 작성과 후속 실험 설계를 위한 기준 문서다.

## 2. 연구 목적

최근에 관측한 동적 공중 객체의 위치 이력으로 15초 대표 경로를 예측하고, 그 경로 주변에 관측 불확실성과 단기 기동 가능성을 반영한 미래 3차원 점유 영역을 생성한다.

핵심 목표는 다음과 같다.

- 여러 미래 경로를 무작위로 생성하지 않고 하나의 해석 가능한 대표 경로를 사용한다.
- 대표 경로 주변의 좁은 점유 영역과 단기 기동 가능성을 포함한 확장 점유 영역을 함께 제공한다.
- 15초 말단에서 점유 영역이 무제한으로 팽창하지 않도록 기동 이탈 계산 시간을 3초로 제한한다.
- 전체 과정은 0.2초마다 새로운 관측 이력을 이용해 다시 계산한다.

## 3. 입력 조건

- 관측 시간: 최근 3초
- 관측 주기: 5 Hz
- 관측점 수: 16개
- 기본 입력: 3차원 위치와 timestamp
- 선택 입력: 객체 식별 여부 또는 식별 신뢰도
- 객체 크기: 모든 객체를 `7 m × 7 m × 7 m` 정육면체로 고정 근사
- 예측 시간: 향후 15초
- 예측 간격: 0.2초
- 미래 시점 수: 75개

정확한 객체 크기는 입력으로 받지 않는다. 객체 식별 정보는 별도의 물리 한계를 선택하는 값이 아니라 GRU가 참고할 수 있는 보조 정보로만 사용한다.

## 4. 시뮬레이션 관측오차

시뮬레이터는 실제 위치에 의도적으로 관측오차를 추가해 관측 위치를 만든다.

\[
\mathbf p_{\mathrm{obs},i}=\mathbf p_{\mathrm{true},i}+\boldsymbol\epsilon_i
\]

예측기는 관측 위치만 사용하며, 실제 위치는 성능 평가에만 사용한다. 실제 위치와 관측 위치의 차이를 온라인 입력으로 사용하면 정답 정보가 누출되므로 금지한다.

관측오차 범위는 다음 우선순위로 정한다.

1. 시뮬레이터가 축별 오차 한계 또는 센서 불확실성을 공개 입력으로 제공하면 최근 16개 값의 축별 최댓값을 사용한다.
2. 오차 한계가 별도로 제공되지 않으면 시뮬레이션에 설정한 축별 오차 한계를 사용한다.

축별 관측오차 반폭은 다음과 같다.

\[
e_{\mathrm{obs},j}=\max_{i=1,\ldots,16}e_{i,j},
\qquad j\in\{x,y,z\}
\]

여기서 `e`는 실제 정답과의 순간 오차가 아니라 예측기에 공개된 오차 한계 또는 불확실성 값이다.

## 5. GRU 대표 경로 예측

GRU는 최근 3초의 관측 위치 이력을 입력받아 향후 15초 동안의 대표 경로 한 개를 출력한다.

\[
\hat{\mathbf P}_{\mathrm{GRU}}
=
\left[
\hat{\mathbf p}(0.2),
\hat{\mathbf p}(0.4),
\ldots,
\hat{\mathbf p}(15.0)
\right]
\in\mathbb R^{75\times3}
\]

이 구조는 CVAE나 다중 seed 방식이 아니다. GRU가 여러 미래 경로 또는 최종 점유 영역을 직접 출력하지 않고, 점유 영역의 중심이 되는 대표 경로 한 개만 예측한다.

## 6. 기동 이탈 범위 계산

### 6.1 계산용 위치 이력

관측오차가 포함된 위치를 그대로 두 번 차분하면 작은 위치 오차가 큰 가속도로 증폭될 수 있다. 따라서 속도와 가속도를 계산하는 분기에서만 위치 이력을 평활화한다. GRU 입력과 성능 평가용 원자료는 별도로 유지한다.

평활화 방법과 강도는 아직 확정하지 않았으며, 실제 기동을 지나치게 제거하지 않는 조건으로 실험에서 결정한다.

### 6.2 속도와 가속도

평활화된 위치 16개에서 속도 15개를 계산한다.

\[
\mathbf v_i=
\frac{\mathbf p_i-\mathbf p_{i-1}}
{t_i-t_{i-1}}
\]

속도 15개에서 가속도 14개를 계산한다.

\[
\mathbf a_i=
\frac{\mathbf v_i-\mathbf v_{i-1}}
{t_i-t_{i-1}}
\]

### 6.3 최근 기동의 최댓값 사용

중앙값이나 대표 가속도를 따로 추정하지 않는다. 최근 3초 동안 계산한 가속도 14개에서 축별 음의 방향 최솟값과 양의 방향 최댓값을 각각 사용한다.

\[
a_j^-=\min(0,a_{1,j},\ldots,a_{14,j})
\]

\[
a_j^+=\max(0,a_{1,j},\ldots,a_{14,j})
\]

절댓값의 최댓값 하나를 양쪽에 동일하게 적용하지 않는다. 가속과 감속의 부호를 유지해 대표 경로 양쪽의 이탈 범위를 비대칭적으로 만든다.

### 6.4 이탈 거리

예측 시점 `τ`에 적용할 기동 계산 시간은 최대 3초로 제한한다.

\[
h(\tau)=\min(\tau,3\,\mathrm{s})
\]

축별 이탈 거리의 기본 계산은 다음과 같다.

\[
d_j^-(\tau)=\frac{1}{2}a_j^-h(\tau)^2
\]

\[
d_j^+(\tau)=\frac{1}{2}a_j^+h(\tau)^2
\]

따라서 0.2초부터 3초까지는 기동 이탈 범위가 증가하고, 3초 이후부터 15초까지는 더 증가하지 않는다.

### 6.5 감속과 역방향 이동 제한

감속 성분은 현재 진행 방향을 기준으로 해석한다. 감속 계산으로 진행 속도가 0보다 작아지는 것은 허용하지 않는다.

\[
v_{\mathrm{next}}=\max(0,v_{\mathrm{current}}+a_{\parallel}\Delta t)
\]

물체가 정지한 이후에는 해당 위치를 유지한다. 별도의 반전 기동이 관측되지 않는 한 계산 과정에서 자동으로 후진하거나 반대 방향으로 이동하게 만들지 않는다.

음의 가속도는 기존 점유 영역을 제거하는 값이 아니다. 대표 경로보다 덜 이동할 수 있는 영역을 감속 방향에 추가한다.

## 7. 미래 점유 영역

### 7.1 대표 경로 점유 회랑

대표 경로 점유 회랑은 GRU 대표 경로에 관측오차와 고정 객체 크기를 적용해 생성한다.

축별 기본 반폭은 다음과 같다.

\[
w_{\mathrm{RP},j}=3.5\,\mathrm m+e_{\mathrm{obs},j}
\]

### 7.2 기동 확장 점유 회랑

기동 확장 점유 회랑은 대표 경로 점유 회랑에 기동 이탈 범위를 추가해 생성한다.

\[
I_{\mathrm{ME},j}(\tau)=
\left[
-w_{\mathrm{RP},j}+d_j^-(\tau),
\;w_{\mathrm{RP},j}+d_j^+(\tau)
\right]
\]

기동 이탈 구간에는 항상 `0`이 포함되므로 대표 경로 점유 회랑은 기동 확장 점유 회랑 안에 포함된다.

\[
O_{\mathrm{RP}}(\tau)\subseteq O_{\mathrm{ME}}(\tau)
\]

감속하거나 정지하더라도 점유 영역은 사라지지 않는다. 이동하지 않는 미래 시점들의 점유 영역이 같은 위치에 겹쳐 나타날 뿐이다.

## 8. 전체 처리 흐름

```text
최근 3초 관측 위치 16개
          │
          ├─ 상대좌표 변환 ──> GRU ──> 15초 대표 경로 75개
          │
          ├─ 공개된 관측오차 이력 ──> 축별 관측오차 범위
          │
          └─ 평활화 ──> 속도 15개 ──> 가속도 14개
                                      └─> 축별 최솟값·최댓값
                                                └─> 3초 기동 이탈 범위

GRU 대표 경로
    ├─ + 관측오차 + 7 m 고정 정육면체
    │       └─> 대표 경로 점유 회랑 O_RP
    │
    └─ + 관측오차 + 7 m 고정 정육면체 + 기동 이탈 범위
            └─> 기동 확장 점유 회랑 O_ME
```

새 관측이 들어오는 0.2초마다 위 과정을 다시 수행한다. 실제 물체가 기존 회랑에서 벗어나면 새로운 관측 이력을 기준으로 대표 경로와 점유 회랑을 다시 생성하고, 필요한 경우 이탈 시점을 기준으로 기존 회랑과 새 회랑을 연결한다.

## 9. 이 설계에서 사용하지 않는 것

- CVAE 또는 무작위 seed 기반 다중 경로 생성
- 상위 27개 경로 선택
- 3×3×3 방향 격자
- 기동 이탈 범위를 예측하는 두 번째 GRU
- 별도의 물리 도달집합 또는 formal Reachability Analysis
- GRU의 최종 점유 영역 직접 출력
- 실제 위치나 시뮬레이터 내부 정답의 온라인 입력 사용

## 10. 해석상의 한계

- 기동 확장 점유 회랑은 최근 3초에 관측된 최대 기동을 바탕으로 만든 경험적 범위다.
- 아직 관측되지 않은 더 강한 기동을 모두 포함한다고 보장하지 않는다.
- 두 점유 회랑은 보정된 확률 영역이나 항공 안전을 보증하는 영역이 아니다.
- 최댓값 방식은 이상치에 민감하므로 평활화 방법과 이상치 처리 규칙을 실험으로 검증해야 한다.
- 3초 이후 폭을 고정하는 것은 장기 팽창을 억제하기 위한 설계 선택이며, 15초 전체의 물리적 도달 가능성을 완전하게 포함한다는 뜻이 아니다.

## 11. 아직 실험으로 정할 항목

- 위치 평활화 방법과 적용 강도
- 시뮬레이션 관측오차의 분포와 축별 크기
- GRU 층수, hidden size, dropout 등 학습 설정
- 객체 식별 정보의 입력 표현
- 가속도 이상치 처리 기준
- 대표 경로와 두 점유 회랑의 평가 지표 및 합격 기준
- 회랑 이탈 후 기존 회랑과 새 회랑을 연결하는 구체적인 시간 정렬 방법

## 12. 논문용 핵심 설명

> 최근 3초간 관측한 동적 공중 객체의 위치 이력으로 GRU가 향후 15초의 대표 경로를 예측한다. 대표 경로에 관측오차와 고정 객체 크기를 적용해 대표 경로 점유 회랑을 생성하고, 최근 위치 이력에서 계산한 축별 최대 가속 및 감속이 최대 3초 동안 지속될 때의 이탈 거리를 추가해 기동 확장 점유 회랑을 구성한다. 전체 예측은 새로운 관측이 입력될 때마다 갱신한다.

